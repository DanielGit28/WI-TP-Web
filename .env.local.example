# URL of the deployed WI-TP backend (Cloud Run), no trailing slash.
# Local dev against a tunnel (ngrok/etc) works the same way the backend's
# own README describes for APP_BASE_URL.
NEXT_PUBLIC_API_URL=https://your-cloud-run-url.a.run.app
